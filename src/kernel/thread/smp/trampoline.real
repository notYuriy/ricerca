bits 16

org 0x0000

jmp 0x7100:start16

start16:
	xor ax, ax
	mov ds, ax
	mov es, ax
	mov fs, ax
	mov gs, ax
	mov ss, ax

	mov eax, 10100000b
	mov cr4, eax
 
	mov edx, dword [0x7ff8]
	mov cr3, edx

	jmp $
